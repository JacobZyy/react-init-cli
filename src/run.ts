export interface CliRunOptions {
  /**
   * use the default template: react-init-template
   */
  yes?: boolean
  /**
   * figure the React project template
   */
  template?: string
}

export async function run(args: CliRunOptions) {

}
